# AUDIT A4 — erreurs client (scan des logs en jeu)

> AUTO-GÉNÉRÉ par `tools/audit/audit.sh a4` — 2026-08-24T21:12Z. Scan de `/tmp/dh_run*.log` + `/tmp/dh_game.log` (dernières exécutions).
> Bénins CONNUS exclus (stat warnings, trader INVASION, XDG/layout headless — cf. SHIMS). Chiffres normalisés (`N`) + dédup.
> Logs scannés : /tmp/dh_run_store.log
/tmp/dh_game.log
/tmp/dh_run_hooks.log
/tmp/dh_run_gapC.log
/tmp/dh_run_gapB.log
/tmp/dh_run_cb2.log
/tmp/dh_run_cb.log
/tmp/dh_run_ct3.log

**Aucune erreur client non-bénigne détectée** dans les logs scannés. ✅

(Pour un scan à jour : lancer la pile, rejouer les écrans à auditer, puis relancer `audit.sh a4`.)
